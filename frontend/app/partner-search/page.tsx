"use client"

import { PartnerSearchView } from "@/components/partner-search/PartnerSearchView"

/**
 * SFR-021 · Agent 협약기관 검색 목업 페이지.
 * 실제 로직은 `components/partner-search/PartnerSearchView` 에 위임한다.
 */
export default function PartnerSearchPage() {
  return <PartnerSearchView />
}
